1. 后代选择器
	元素1 元素2 元素3 ... {
		样式声明
	}
	表示：选择元素1中所有的元素2
	元素1和元素2中间要用空格隔开
	多个元素相当于文件夹逐层路径，但是如果不会产生歧义，也可以省略中途某些路径

2. 子选择器
	元素1>元素2 {
		样式声明
	}
	只会选出亲儿子，亲孙子亲重孙子都不会被选出

3. 并集选择器
	元素1, 
	元素2,
	... {
		样式声明
	}
	用于几个不同标签需要改成同一样式
	语法规范习惯竖着写，最后一个元素不加逗号
